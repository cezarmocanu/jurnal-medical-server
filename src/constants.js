/* OP stants for operations */
const OP_PATHS = {
    GET_ONE_BY_ID: '/one/:id',
    GET_ALL: '/all',
    CREATE_ONE: '/create/one',
    UPDATE: '/update/:id',
    DELETE: '/delete/:id'
};

module.exports = {
    OP_PATHS
}